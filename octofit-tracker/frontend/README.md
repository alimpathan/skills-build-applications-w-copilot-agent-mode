OctoFit Tracker — Frontend

This folder will contain the React frontend for OctoFit Tracker.

Quick start (when ready):

```bash
# from repo root
cd octofit-tracker/frontend
# install dependencies (requires Node.js and npm)
npm install
npm start
```

If you'd prefer a different stack (Vite, Next.js), we can scaffold that instead.
